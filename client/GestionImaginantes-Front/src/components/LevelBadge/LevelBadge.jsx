import React from 'react';
import { Box } from '@mui/material';

import './styles.css'

const LevelBadge = () => {
    return (
        <Box className="level-badge-card-container" sx={{ boxShadow: "0px 0px 5px rgba(0, 0, 0, 0.2)"}}>
        </Box>
    );
}

export default LevelBadge;